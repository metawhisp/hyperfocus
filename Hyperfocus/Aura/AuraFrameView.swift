// AuraFrameView.swift — gradient glow drawn per screen edge (canon §3).

import SwiftUI

struct AuraFrameView: View {
    var body: some View {
        // IMPLEMENT — see specs/05-implementation-plan.md Phase 5
        Text("AuraFrameView")
    }
}

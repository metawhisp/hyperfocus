// FocusOrbView.swift — SwiftUI orb visuals per orb visual state (canon §2).

import SwiftUI

struct FocusOrbView: View {
    var body: some View {
        // IMPLEMENT — see specs/05-implementation-plan.md Phase 2
        Text("FocusOrbView")
    }
}

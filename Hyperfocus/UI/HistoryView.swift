// HistoryView.swift — simple list of past sessions: date, mission, duration, status, breaks (canon §2).

import SwiftUI

struct HistoryView: View {
    var body: some View {
        // IMPLEMENT — see specs/05-implementation-plan.md Phase 10
        Text("HistoryView")
    }
}

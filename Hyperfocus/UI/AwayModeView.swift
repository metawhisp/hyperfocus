// AwayModeView.swift — "Session paused" glass card with Return / Exit Session buttons (canon §9).

import SwiftUI

struct AwayModeView: View {
    var body: some View {
        // IMPLEMENT — see specs/05-implementation-plan.md Phase 8
        Text("AwayModeView")
    }
}

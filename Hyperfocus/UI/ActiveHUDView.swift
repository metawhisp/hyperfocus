// ActiveHUDView.swift — mission / remaining time / camera status HUD shown during an active session (canon §9).

import SwiftUI

struct ActiveHUDView: View {
    var body: some View {
        // IMPLEMENT — see specs/05-implementation-plan.md Phase 6
        Text("ActiveHUDView")
    }
}

// OnboardingView.swift — 5-screen first-launch onboarding flow (canon §9).

import SwiftUI

struct OnboardingView: View {
    var body: some View {
        // IMPLEMENT — see specs/05-implementation-plan.md Phase 10
        Text("OnboardingView")
    }
}
